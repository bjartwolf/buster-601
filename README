This tutorial is the tutorial from http://learn.knockoutjs.com/#/?tutorial=collections
with setup and tests from busterjs.org

They require buster, node, npm to be set up on your system

Link to buster
	sudo npm link buster
Install buster lint
	sudo npm install buster-lint

Start dummy server
start a new terminal and run 
node server.js


Run tests
	buster server
	// Connect browser(s) to localhost:1111
	buster test
